import { WorkerSearchPipe } from './worker-search.pipe';

describe('WorkerSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkerSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
